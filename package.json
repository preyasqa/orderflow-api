{
  "name": "orderflow-api",
  "version": "1.4.2",
  "description": "REST API that manages order creation, payment processing, and fulfillment status for the retail platform.",
  "main": "src/index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --coverage",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "stripe": "^15.7.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^9.5.0"
  }
}
